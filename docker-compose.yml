version: '3.9'
services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: skeleton
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    networks:
      - default
    volumes:
      - pg-data:/var/lib/postgresql/data

  server:
    image: ${SERVER_IMAGE:-skeleton-server}
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    depends_on:
      - db
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://postgres:postgres@db:5432/skeleton
      # In preview, the webapp lives at a subdomain.
      # We use the COMPOSE_PROJECT_NAME (e.g. 'feature-login') to build the URL.
      WEB_URL: http://${COMPOSE_PROJECT_NAME}.${HOST_DOMAIN:-lvh.me}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:4000/healthz" ]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - default
      - web_proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web_proxy"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.rule=Host(`${COMPOSE_PROJECT_NAME}.${HOST_DOMAIN:-lvh.me}`) && PathPrefix(`/trpc`, `/healthz`)"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.entrypoints=web,websecure"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-api.tls.certresolver=myresolver"
      - "traefik.http.services.${COMPOSE_PROJECT_NAME}-api.loadbalancer.server.port=4000"

  web:
    image: ${WEB_IMAGE:-skeleton-web}
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    depends_on:
      - server
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 128M
    networks:
      - default
      - web_proxy
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web_proxy"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-web.rule=Host(`${COMPOSE_PROJECT_NAME}.${HOST_DOMAIN:-lvh.me}`)"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-web.entrypoints=web,websecure"
      - "traefik.http.routers.${COMPOSE_PROJECT_NAME}-web.tls.certresolver=myresolver"
      - "traefik.http.services.${COMPOSE_PROJECT_NAME}-web.loadbalancer.server.port=80"

volumes:
  pg-data:


networks:
  web_proxy:
    external: true
