{
  "name": "skeleton3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@8.15.8",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:generate": "pnpm --filter @acme/db db:generate",
    "db:migrate": "pnpm --filter @acme/db db:migrate",
    "db:seed": "pnpm --filter @acme/db db:seed"
  },
  "trustedDependencies": [
    "@prisma/client",
    "@prisma/engines",
    "esbuild",
    "prisma"
  ],
  "devDependencies": {
    "@types/node": "^20.14.10",
    "jsdom": "^24.1.0",
    "turbo": "^2.1.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
